---
import BaseLayout from "../../layouts/BaseLayout.astro";
import PageHeader from "../../components/PageHeader.astro";
import TermCloud from "../../components/TermCloud.astro";
import { getAllPosts, getCollectionMaterialColorStyle, getTerms } from "../../lib/posts";
import { siteConfig } from "../../lib/site";

const terms = getTerms(await getAllPosts(), "series");
const taxonomyConfig = siteConfig.taxonomy ?? {};
---

<BaseLayout title="Series" description="All post series and collections.">
  {taxonomyConfig.showHero !== false && <PageHeader eyebrow="Series" title="Series" description="Series are organized through front matter `series` and `seriesOrder` fields." style={getCollectionMaterialColorStyle("series")} />}
  <section class="content-section">
    <TermCloud terms={terms} basePath="/series" kind="series" showCount={taxonomyConfig.showTermCount !== false} cardView={taxonomyConfig.cardView === true} />
  </section>
</BaseLayout>
